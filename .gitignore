# dependencies
node_modules/
# also ignore a root node_modules symlink (the sandbox/Docker build links it
# to server/ or web/ node_modules; a trailing-slash pattern won't match a symlink)
node_modules

# build output
dist/
server/dist/

# runtime data (uploads, outputs, sqlite db)
data/

# logs & env
*.log
.env
.env.local
.env.*.local

# OS
.DS_Store
__MACOSX/

web/dist/
web/node_modules/

# Local test media for cutter algorithm dev (not committed)
lab/server/src/scripts/fixtures/
lab/server/src/scripts/analyze-sample.ts
lab/server/src/scripts/dev-transcribe.ts
