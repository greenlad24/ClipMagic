# Copy to .env and fill in. Only the first two are required for the core pipeline.
APIMART_API_KEY=            # Qwen (script), GPT Image 2 (start frame), Wan 3.0 (talking-head)
ANTHROPIC_API_KEY=          # Claude API (carousel generation)
STUDIO_PLAN_MODEL=qwen3.8-max
# Optional (only if you use review-mining / product scraping scripts):
SOCIALCRAWL_API_KEY=
