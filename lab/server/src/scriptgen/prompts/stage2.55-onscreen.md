You are finishing a Jake Dawson video outline. The outline is written; the video it describes has not been shot yet.

Some sections arrived without saying what the viewer will be LOOKING AT while Jake talks. That line is not decoration — the section writers only ever see this outline, and a section that reaches them with nothing to show gets written as an essay paragraph. That is where viewers leave.

Your job: write the missing `ON SCREEN:` line for each section named below. One line each. Nothing else changes.

## WHAT AN ON SCREEN LINE IS

A physical description of a thing changing on a screen, during that section.

- Good: `the three clips play side by side, same prompt, visibly different light and motion in each`
- Good: `the packaging mockup appears with the same bottle and the same logo from the two earlier images`
- Good: `the tidy dated journal file pops up in the vault's left panel, seconds after the messy paste`
- Bad: `demonstration of the search feature` — that is a topic, not a moment
- Bad: `Claude's response` — a response to what, showing what changed?

Rules:
- Describe what CHANGES, not what the feature is for. The viewer can already see the interface; they cannot see what just became different.
- Use only what the outline and the research actually support. Never invent a screen, a menu, or a result that nobody has established exists.
- If a section genuinely has nothing to show — pricing, the honest limits, privacy — the correct answer is exactly: `none — this is a trust beat, Jake to camera`. Use it when it is true, and not to avoid thinking.

## OUTPUT

Respond as STRICT JSON only, no markdown, no commentary:

{"lines": {"<section name, copied exactly as given below>": "<the one line>"}}

One entry per section named. Section names must match the given ones character for character, or the line will be dropped.

THE SECTIONS MISSING A LINE:
[SECTION NAMES]

THE OUTLINE:
[PASTE THE OUTLINE]
